spring:
  application:
    name: auth-service
  config:
    import: "configserver:"
  cloud:
    config:
      uri: https://github.com/hades-persephone/hadestack-netflix-cloud-server.git
      label: main
      profile: dev